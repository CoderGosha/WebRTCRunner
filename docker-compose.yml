services:
  webrtc-runner:
    build: .
    network_mode: host
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./cookies:/app/cookies/local:ro
      - webrtc_runner_cookies:/app/cookies/volume

volumes:
  webrtc_runner_cookies:
